package collectionimpl;

import com.sun.source.tree.Tree;

import java.util.*;

public class LRU {
    int capacity;
    Queue<Integer> queue;
    Map<Integer, Integer> myMap;
    public LRU(int capacity) {
        myMap= new HashMap<>();
        myMap.putIfAbsent()
                Set<Integer> myval = new TreeSet<>();

        Integer [] myarr = myval.toArray(new Integer[0]);
    }
}
